{
  "name": "una-platform-lite",
  "version": "1.0.0",
  "scripts": {
    "dev": "cd una-platform && npm run dev",
    "dev:clean": "./scripts/start-clean.sh",
    "build": "cd una-platform && npm run build",
    "install:all": "npm install && cd una-platform && npm install"
  },
  "dependencies": {
    "lucide-react": "^0.344.0"
  }
}
